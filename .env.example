# --- LLM ---
# API key for whichever model provider you use in src/config.py
ANTHROPIC_API_KEY=your_key_here

# --- MCP server ---
# Point at the real Robinhood Trading MCP once you have Agentic Trading
# access, or at the local mock server (mock_mcp/server.py) during
# development. NEVER commit a real, authenticated session/token here.
MCP_SERVER_URL=http://localhost:8000/mcp

# --- Logging ---
DECISION_LOG_PATH=logs/decisions.jsonl
